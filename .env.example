# Copy to .env (gitignored). Only needed for regenerating game art —
# the game itself runs without it.
#
# Used by scripts/assetlib.py / scripts/generate_assets.py:
#   pip install google-genai pillow numpy scipy
#   python3 scripts/generate_assets.py            # everything (costs $$)
#   python3 scripts/generate_assets.py pip goods  # single families
GEMINI_API_KEY=your-gemini-api-key-here
